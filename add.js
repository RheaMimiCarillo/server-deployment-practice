'use strict';

// write a function that adds 2 numbers

module.exports = (a,b) => 
{
  return a+b;
}

/* What is Express for?
  express is for using the HTTP RESTful framework
  REpresentational State Transfer

  Requirements for HTTP REST request:
    1. URL (path)
    2. Method: PUT / GET / POST / DELETE 

  Requirements for HTTP REST Response:
    1. Endpoint
    2. 
*/
